# crypto-strat-analysis

```
Because Krakenex Requires Python3 Usage, this module can only be run with Python3

To Install Necessary Libs (Alternatively Run the libSetup script):

pip3 install coinbase
pip3 install krakenex
pip3 install poloniex
pip3 install --upgrade six

```
